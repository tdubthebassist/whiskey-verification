{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd admin && npm install && npm run build && cd .. && node scripts/prepare-dist.js"
  },
  "deploy": {
    "startCommand": "npx serve dist -l $PORT -s"
  }
}
